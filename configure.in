AC_INIT(art_misc.h)
AM_CONFIG_HEADER(config.h)
AM_INIT_AUTOMAKE(libart_lgpl,0.0.0)

AC_PROG_CC
AC_PROG_CPP
AM_PROG_LIBTOOL

AM_MAINTAINER_MODE

LIBART_LIBS=$LIBS
AC_SUBST(LIBART_LIBS)

AC_OUTPUT([
Makefile
libart-config], [case "$CONFIG_FILES" in *libart-config*) chmod +x libart-config;; esac])
